{
  "name": "logisticitrack",
  "version": "1.0.0",
  "description": "Provides order information back if available given an Order ID in a text string",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "openai": "^4.2.0"
  }
}
